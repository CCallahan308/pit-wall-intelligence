# Copy to .env and fill in.

# Optional — Visual Crossing API key for richer weather backfill.
# Open-Meteo is used as the free default.
VISUAL_CROSSING_API_KEY=

# Where to keep the FastF1 cache. Defaults to ./cache/ if unset.
PITWALL_CACHE_DIR=
