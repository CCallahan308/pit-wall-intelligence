name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true

      - name: Install Python
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync --extra dev

      - name: Lint
        # Lint everywhere we have first-party code, not just src/ tests/.
        # Previously app/ scripts/ api/ would slip through CI and only get
        # caught locally.
        run: |
          uv run ruff check src/ tests/ app/ scripts/ api/
          uv run ruff format --check src/ tests/ app/ scripts/ api/

      - name: Type check
        run: uv run mypy src/ || true   # informational for now

      - name: Run pytest
        run: uv run pytest tests/ -v --tb=short
